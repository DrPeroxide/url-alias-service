# URL Alias Service

## Build & Deploy

### Prerequisites

- Docker desktop is running

1. Build the service by running `docker compose -p url-alias-service build`
2. Run the service by running `docker compose -p url-alias-service up -d`
3. Shut down the service with `docker compose -p url-alias-service down`.

